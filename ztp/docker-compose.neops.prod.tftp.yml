---
services:
  tftpd:
    image: quay.io/zebbra/neops-atftpd:latest
    network_mode: "host"
    expose:
      - 69/udp
    environment:
      DEBUG: "${DEBUG}"
    restart: "on-failure"
    volumes:
      - ./data/tftp:/tftp

networks:
  private:
    driver: bridge
